spring:
  redis:
    host: ${SKY_REDIS_HOST:192.168.88.130}
    port: ${SKY_REDIS_PORT:6379}
    password: ${SKY_REDIS_PASSWORD:123456}
    lettuce:
      pool:
        max-active: 300
        max-idle: 300
        min-idle: 10
        time-between-eviction-runs: 10s

sky:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    host: 127.0.0.1
    port: 3306
    database: sky_take_out
    username: root
    password: 123456
  alioss:
    endpoint: ${ALIYUN_OSS_ENDPOINT:oss-cn-beijing.aliyuncs.com}
    access-key-id: ${ALIYUN_OSS_ACCESS_KEY_ID}
    access-key-secret: ${ALIYUN_OSS_ACCESS_KEY_SECRET}
    bucket-name: ${ALIYUN_OSS_BUCKET_NAME:sky-wjh}

  wechat:
    appid: ${WECHAT_APPID:wxe8d5c511123a5871}
    # 密钥必须通过环境变量 WECHAT_SECRET 注入，禁止在配置文件中硬编码
    secret: ${WECHAT_SECRET:febde6ded196f1a6177d1c70720556af}
