<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.sky.mapper.SetmealMapper">


    <!--
        新增套餐

        useGeneratedKeys="true"
        表示获取数据库自动生成的主键

        keyProperty="id"
        表示把主键赋值给 setmeal.id
    -->
    <insert id="insert"
            parameterType="com.sky.entity.Setmeal"
            useGeneratedKeys="true"
            keyProperty="id">

        insert into setmeal
        (
            category_id,
            name,
            price,
            status,
            description,
            image,
            create_time,
            update_time,
            create_user,
            update_user
        )

        values
            (
                #{categoryId},
                #{name},
                #{price},
                #{status},
                #{description},
                #{image},
                #{createTime},
                #{updateTime},
                #{createUser},
                #{updateUser}
            )

    </insert>



    <!--
        套餐分页查询
    -->
    <select id="pageQuery"
            resultType="com.sky.vo.SetmealVO">

        select
        s.*,
        c.name categoryName

        from setmeal s

        left join category c
        on s.category_id = c.id

        <where>

            <!-- 根据套餐名称模糊查询 -->
            <if test="name != null and name != ''">

                and s.name like concat('%', #{name}, '%')

            </if>


            <!-- 根据套餐状态查询 -->
            <if test="status != null">

                and s.status = #{status}

            </if>


            <!-- 根据分类id查询 -->
            <if test="categoryId != null">

                and s.category_id = #{categoryId}

            </if>

        </where>

        order by s.create_time desc

    </select>



    <!--
        动态修改套餐

        为什么必须动态修改？

        因为起售停售的时候只传：

        id
        status

        其他字段都是 null。

        如果直接全部 update，
        其他字段可能被更新成 null。
    -->
    <update id="update"
            parameterType="com.sky.entity.Setmeal">

        update setmeal

        <set>

            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>

            <if test="name != null">
                name = #{name},
            </if>

            <if test="price != null">
                price = #{price},
            </if>

            <if test="status != null">
                status = #{status},
            </if>

            <if test="description != null">
                description = #{description},
            </if>

            <if test="image != null">
                image = #{image},
            </if>

            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>

            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>

        </set>

        where id = #{id}

    </update>



    <!--
        动态条件查询套餐
    -->
    <select id="list"
            resultType="com.sky.entity.Setmeal">

        select * from setmeal

        <where>

            <if test="categoryId != null">

                and category_id = #{categoryId}

            </if>

            <if test="status != null">

                and status = #{status}

            </if>

        </where>

        order by create_time desc

    </select>


</mapper>